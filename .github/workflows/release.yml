# Build macOS DMG + Windows zip and attach to GitHub Release on every version tag (v*).
name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-macos:
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-latest
            asset_suffix: arm64
          - runner: macos-15-intel
            asset_suffix: x86_64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m venv .venv
          .venv/bin/pip install --upgrade pip
          .venv/bin/pip install -r requirements.txt
          .venv/bin/pip install pyinstaller

      - name: Verify tkinter
        run: .venv/bin/python -c "import tkinter; print('tkinter OK')"

      - name: Build DMG
        run: ./scripts/build_release.sh

      - name: Stage release asset
        run: |
          DMG="MIK-to-Rekordbox-${{ matrix.asset_suffix }}.dmg"
          cp dist/MIK-to-Rekordbox.dmg "dist/${DMG}"

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: dmg-${{ matrix.asset_suffix }}
          path: dist/MIK-to-Rekordbox-*.dmg
          if-no-files-found: error

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build Windows release
        shell: pwsh
        run: ./scripts/build_release_win.ps1

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: win-x86_64
          path: dist/MIK-to-Rekordbox-win64.zip
          if-no-files-found: error

  publish:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download release assets
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/**/*
          generate_release_notes: true
          draft: false
